repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: check-merge-conflict
        name: Dectect merge conflicts
      - id: end-of-file-fixer
        name: Fix end of file
      - id: trailing-whitespace
        name: Delete trailing whitespace
      - id: mixed-line-ending
        name: Check end of line
        args: ["--fix=lf"]
      - id: check-case-conflict
        name: Check case conflict

  - repo: https://github.com/doublify/pre-commit-rust
    rev: v1.0
    hooks:
      - id: fmt
        name: Format with rustfmt
        args: ["--manifest-path", "Cargo.toml", "--"]
      - id: cargo-check
        name: Check compilation
        args: ["--all-features"]
      - id: clippy
        name: Check with Clippy
        args: ["--all-targets", "--all-features", "--", "-D", "warnings"]

  - repo: local
    hooks:
      - id: cargo-llvm-cov
        name: Execute test with coverage
        entry: bash -c 'TERM=dumb cargo llvm-cov --all-features --workspace --color=never'
        language: system
        types: [rust]
        pass_filenames: false

      - id: cargo-doc
        name: Check documentation
        entry: cargo doc
        language: system
        types: [rust]
        pass_filenames: false
        args: ["--no-deps", "--document-private-items", "--all-features"]

      - id: cargo-audit
        name: Security audit
        entry: bash -c 'if command -v cargo-audit &> /dev/null; then cargo audit; else echo "⚠️  cargo-audit not installed, skip security audit"; fi'
        language: system
        types: [rust]
        pass_filenames: false
        verbose: true

      - id: cargo-udeps
        name: unused dependencies
        entry: bash -c '
          nightly_toolchain=$(command -v rustup &> /dev/null && rustup toolchain list | grep nightly | head -n1 | awk "{print \$1}");
          if command -v cargo-udeps &> /dev/null && [ -n "$nightly_toolchain" ]; then
          echo "🔍 Check unused dependencies with $nightly_toolchain...";
          cargo "+$nightly_toolchain" udeps --all-targets;
          else
          echo "⚠️  cargo-udeps and/or a nightly toolchain (via rustup) are not available, skip unused dependencies check";
          fi '
        language: system
        types: [rust]
        pass_filenames: false
        verbose: true

      - id: no-debug-prints
        name: Detection of println! and dbg!
        entry: bash -c 'if git diff --cached --name-only | grep "\.rs$" | xargs grep -n "println!\|dbg!" 2>/dev/null; then echo "⚠️  println! or dbg! detected"; exit 1; fi'
        language: system
        types: [rust]
        pass_filenames: false

      - id: cargo-lock
        name: Check Cargo.lock
        entry: cargo update --dry-run
        language: system
        types: [rust]
        pass_filenames: false

default_install_hook_types: [pre-commit, pre-push]
default_stages: [pre-commit]

exclude: |
  (?x)^(
      target/|
      .git/|
      .cargo/|
      .*/frames/.*\.adoc$
  )$

ci:
  autofix_commit_msg: |
    [pre-commit.ci] auto fixes from pre-commit hooks
  autofix_prs: true
  autoupdate_branch: ""
  autoupdate_commit_msg: "[pre-commit.ci] pre-commit autoupdate"
  autoupdate_schedule: weekly
  skip: []
  submodules: false
